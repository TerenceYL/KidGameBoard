<Application
    x:Class="KidGameBoard.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:conv="clr-namespace:KidGameBoard.Converters"
    xmlns:local="clr-namespace:KidGameBoard"
    StartupUri="MainWindow.xaml">
    <Application.Resources>
        <conv:WorkItemCheckedMultiConverter x:Key="WorkItemCheckedMultiConverter" />
     


        <FontFamily x:Key="C39P24DmTt">
            ./Fonts/#C39P24DmTt
        </FontFamily>
        <FontFamily x:Key="3of9">
            ./Fonts/#3 of 9 Barcode
        </FontFamily>
        <FontFamily x:Key="jfopen">
            ./Fonts/#jf open 粉圓 2.0
        </FontFamily>

        <Style
            x:Key="DataGridStyle"
            BasedOn="{StaticResource {x:Type DataGrid}}"
            TargetType="DataGrid">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="RowBackground" Value="LightCyan" />
            <Setter Property="AlternatingRowBackground" Value="PowderBlue" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="SelectionMode" Value="Single" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="IsEnabled" Value="False" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="False" />
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsQueryButtonEnable}" Value="True">
                    <Setter Property="IsEnabled" Value="True" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style
            x:Key="DataGridCellStyle"
            BasedOn="{StaticResource {x:Type DataGridCell}}"
            TargetType="DataGridCell">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="TextBlock.TextAlignment" Value="Center" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="MaxHeight" Value="100" />
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="Blue" />
                    <Setter Property="Foreground" Value="White" />
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style
            x:Key="DataGridHeaderStyle"
            BasedOn="{StaticResource {x:Type DataGridColumnHeader}}"
            TargetType="DataGridColumnHeader">
            <Setter Property="HorizontalContentAlignment" Value="Center" />
        </Style>
        <Style
            x:Key="ComboBoxStyle"
            BasedOn="{StaticResource {x:Type ComboBox}}"
            TargetType="ComboBox">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style
            x:Key="ButtonStyle"
            BasedOn="{StaticResource {x:Type Button}}"
            TargetType="Button">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Margin" Value="5" />

            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Background" Value="#007ACC" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect
                        BlurRadius="10"
                        ShadowDepth="1"
                        Color="Aquamarine" />
                </Setter.Value>
            </Setter>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="IsEnabled" Value="False" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3399FF" />
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#005A9E" />
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC" />
                    <Setter Property="Foreground" Value="#777777" />
                </Trigger>
                <DataTrigger Binding="{Binding IsQueryButtonEnable}" Value="True">
                    <Setter Property="IsEnabled" Value="True" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style
            x:Key="ProgressBarStyle"
            BasedOn="{StaticResource {x:Type ProgressBar}}"
            TargetType="ProgressBar">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
        </Style>
        <Style
            x:Key="TextBlockStyle"
            BasedOn="{StaticResource {x:Type TextBlock}}"
            TargetType="TextBlock">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style
            x:Key="LabelStyle"
            BasedOn="{StaticResource {x:Type Label}}"
            TargetType="Label">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style
            x:Key="TextBoxStyle"
            BasedOn="{StaticResource {x:Type TextBox}}"
            TargetType="TextBox">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Background" Value="#BBFFAA" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style
            x:Key="CheckBoxStyle"
            BasedOn="{StaticResource {x:Type CheckBox}}"
            TargetType="CheckBox">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style
            x:Key="RadioButtonStyle"
            BasedOn="{StaticResource {x:Type RadioButton}}"
            TargetType="RadioButton">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Width" Value="60" />
        </Style>
        <Style
            x:Key="PasswordBoxStyle"
            BasedOn="{StaticResource {x:Type PasswordBox}}"
            TargetType="PasswordBox">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Background" Value="#FFF2CC" />
            <Setter Property="Margin" Value="2" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
        <Style
            x:Key="CalendarDayButtonStyle"
            BasedOn="{StaticResource {x:Type CalendarDayButton}}"
            TargetType="CalendarDayButton" />
        <Style
            x:Key="CalendarStyle"
            BasedOn="{StaticResource {x:Type Calendar}}"
            TargetType="Calendar">
            <Setter Property="FontSize" Value="20" />
        </Style>
        <Style
            x:Key="RichTextBoxStyle"
            BasedOn="{StaticResource {x:Type RichTextBox}}"
            TargetType="RichTextBox">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="Margin" Value="5" />
        </Style>
        <Style
            x:Key="DatePickerStyle"
            BasedOn="{StaticResource {x:Type DatePicker}}"
            TargetType="DatePicker">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
        </Style>
        <Style
            x:Key="ToggleButtonStyle"
            BasedOn="{StaticResource {x:Type ToggleButton}}"
            TargetType="ToggleButton">
            <Setter Property="FontFamily" Value="{StaticResource jfopen}" />
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Background" Value="#ffe0e5" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect
                        BlurRadius="10"
                        ShadowDepth="1"
                        Color="Red" />
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect
                                BlurRadius="10"
                                ShadowDepth="1"
                                Color="Blue" />
                        </Setter.Value>
                    </Setter>
                    <Setter Property="Foreground" Value="Blue" />
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style BasedOn="{StaticResource DataGridStyle}" TargetType="DataGrid" />
        <Style BasedOn="{StaticResource DataGridCellStyle}" TargetType="DataGridCell" />
        <Style BasedOn="{StaticResource DataGridHeaderStyle}" TargetType="DataGridColumnHeader" />
        <Style BasedOn="{StaticResource ComboBoxStyle}" TargetType="ComboBox" />
        <Style BasedOn="{StaticResource ButtonStyle}" TargetType="Button" />
        <Style BasedOn="{StaticResource ProgressBarStyle}" TargetType="ProgressBar" />
        <Style BasedOn="{StaticResource TextBlockStyle}" TargetType="TextBlock" />
        <Style BasedOn="{StaticResource LabelStyle}" TargetType="Label" />
        <Style BasedOn="{StaticResource TextBoxStyle}" TargetType="TextBox" />
        <Style BasedOn="{StaticResource CheckBoxStyle}" TargetType="CheckBox" />
        <Style BasedOn="{StaticResource RadioButtonStyle}" TargetType="RadioButton" />
        <Style BasedOn="{StaticResource PasswordBoxStyle}" TargetType="PasswordBox" />
        <Style BasedOn="{StaticResource CalendarDayButtonStyle}" TargetType="CalendarDayButton" />
        <Style BasedOn="{StaticResource CalendarStyle}" TargetType="Calendar" />
        <Style BasedOn="{StaticResource RichTextBoxStyle}" TargetType="RichTextBox" />
        <Style BasedOn="{StaticResource DatePickerStyle}" TargetType="DatePicker" />
        <Style BasedOn="{StaticResource ToggleButtonStyle}" TargetType="ToggleButton" />
    </Application.Resources>
</Application>
